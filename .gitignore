/target

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Whitepaper working directory
.whitepaper/

# Per-host TLS material — private keys must never reach a remote
*.crt
*.key
*.pem
