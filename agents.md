# Agents.md

## Project Info

name: Dockyy
desc: docker dashboard

## Architecture

FE: React (Pure client-side only, no SSR, bun as package manager)
BE: Flask (use uv as package manager and virtual env)